<!-- AGENTS.md is the engineering-standards document; CI runs
     cargo fmt --all --check · cargo clippy --workspace --all-targets -- -D warnings · cargo test --workspace -->

## Why

<!-- The problem or gap this closes. -->

## What

<!-- What changed, at the level a reviewer needs. -->

## Verification

<!-- How you know it works: tests added, and — for anything touching
     input synthesis, permissions, or coordinates — the manual run on
     real hardware, with what you observed. A green suite proves the
     coordinates were computed, never that anything moved. -->
